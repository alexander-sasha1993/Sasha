<%= form_for(employee) do |f| %>
  <% if employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table width="100%">
  <tr>
     <td id="MyLeft">
        Фамилия, Имя и Отчество:
     </td>
     <td id="MyRight">
        <%= f.text_field :family, :size=>"40", :maxlength=>"40" %>
     </td>
  </tr>
  <tr>
     <td id="MyLeft">
        Адрес:
     </td>
     <td id="MyRight">
        <%= f.text_field :adress, :size=>"50", :maxlength=>"50" %>
     </td>
  </tr>
  <tr>
     <td id="MyLeft">
        Телефон:
     </td>
     <td id="MyRight">
        <%= f.text_field :phone, :size=>"15", :maxlength=>"15" %>
     </td>
  </tr>
  <tr>
     <td id="MyLeft">
        Описание:
     </td>
     <td id="MyRight">
        <%= f.text_area :description, :size=>"80x7", :maxlength=>"560" %>
     </td>
  </tr>
  <tr>
     <td id="MyLeft">
        Выберите должность:
     </td>
     <td id="MyRight">
        <%= f.collection_select(:position_id, Position.all, :id, :name) %>
     </td>
  </tr>
</table>
  <br />
  <div class="actions">
    <%= f.submit ( f.object.new_record? ? "Создать" : "Обновить"), class: "btn" %>
  </div>
<% end %>
